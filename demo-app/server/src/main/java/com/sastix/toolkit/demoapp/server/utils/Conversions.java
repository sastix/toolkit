package com.sastix.toolkit.demoapp.server.utils;

import java.util.function.Function;

public interface Conversions {
    /**
     * Example:
     *
     * Function<GeneralCode, GeneralCodeDTO> convertGeneralCodeToDTO = (a) -> {
         final GeneralCodeDTO generalCodeDTO = new GeneralCodeDTO();
         generalCodeDTO.setId(a.getId());
         generalCodeDTO.setDataType(a.getDataType());
         generalCodeDTO.setId(a.getId());
         return GeneralCodeDTO;
         };
     *
     * */

}
